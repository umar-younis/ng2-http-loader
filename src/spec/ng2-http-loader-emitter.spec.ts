import { TestBed, getTestBed } from '@angular/core/testing';
import { EventEmitter } from '@angular/core';
import { Ng2HttpLoaderEmitter } from '../loader';

describe('Ng2HttpLoaderEmitter', () => {
  let emitter: Ng2HttpLoaderEmitter;

  beforeEach(() => {
    emitter = new Ng2HttpLoaderEmitter();
  });
});